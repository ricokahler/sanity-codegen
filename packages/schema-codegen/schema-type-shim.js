// the following file is a re-export to the compiled `schema-type-shim`.
// this shim is used as a target during schema execution (via module-resolver)
module.exports = require('./dist/schema-type-shim');
